import 'package:flutter/material.dart';

class DemoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("神庙"),
      ),
      body: Center(
        child: Text("这一页还没有想好要做什么，别急"),
      ),
    );
  }
}
